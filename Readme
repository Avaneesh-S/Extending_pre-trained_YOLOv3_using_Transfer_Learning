The colab notebooks contain the implementation of using transfer learning to extend pre-trained YOLOv3 object detection model to detect unknown objects without 
re-training on the entire dataset. The initial YOLOv3 model was trained on COCO dataset.
